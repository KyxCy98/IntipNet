name: Build & Release

on:
  push:
    tags:
      - "v*"

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    strategy:
      fail-fast: false  # Agar jika satu gagal, yang lain tetap jalan
      matrix:
        include:
          - arch: amd64
            compiler: g++
            strip: strip
            pkg: ""
          - arch: aarch64
            compiler: aarch64-linux-gnu-g++
            strip: aarch64-linux-gnu-strip
            pkg: "g++-aarch64-linux-gnu binutils-aarch64-linux-gnu"

    steps:
    - name: Checkout repo
      uses: actions/checkout@v4

    - name: Install compilers and tools
      run: |
        sudo apt-get update
        sudo apt-get install -y zip ${{ matrix.pkg }}

    - name: Verify tools
      run: |
        which ${{ matrix.compiler }} || echo "Compiler not found"
        which ${{ matrix.strip }} || echo "Strip not found"

    - name: Build binary
      run: |
        mkdir -p build
        ${{ matrix.compiler }} lib/main/main.cpp lib/main/module/banner.cpp lib/main/module/logger.cpp lib/main/module/message.cpp \
        -o build/intipnet \
        -Wall -Wextra -std=c++17 -Iinclude

    - name: Strip binary
      run: ${{ matrix.strip }} build/intipnet

    - name: Create ZIP archive
      run: |
        cd build
        zip intipnet-linux-${{ matrix.arch }}.zip intipnet
        cd ..

    - name: Upload Release
      uses: softprops/action-gh-release@v1
      with:
        files: build/intipnet-linux-${{ matrix.arch }}.zip
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
